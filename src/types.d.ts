// Custom Types

// Memory Extension so TS doesn't get mad at me
interface CreepMemory {
  role: string,
  seed: number,
  room: string,
  working: boolean
}

//interface Structure {
  //store: number
//}
