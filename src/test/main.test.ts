import { loop } from "../main";
//import { Game, Memory } from "./mock.test";
import assert from "assert";
//import { expect } from "chai";

describe( "main.ts", function() {

  beforeEach( function() {
  });

  it( "loop should be a function", function() {
    assert( typeof( loop ) === "function", "game loop is function" );
  });

  it( "Spawns a creep at 300 resources", function() {
  });

});
